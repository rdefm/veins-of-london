# reference/

Prose quarry only — per `CLAUDE.md`, never a source of mechanics/formulas/data
(`docs/REFERENCE.md` is canonical for those).

`london-orichalchum.html` (the original HTML prototype) is **not yet in this
repo**. It's needed here for two things:

1. T00's copy-in step (this file stands in for it until it arrives).
2. T13's prose extraction — the tutorial event cards and SMS scripts in
   `docs/CONTENT-GUIDE.md` §2 are extracted verbatim from named consts in
   that file (`INTRO_CARDS`, `BUYER_CARDS`, etc.).

Once supplied, drop it in as `reference/london-orichalchum.html` and this
README can go.
