# 43 — Verify/polish raid-defend-on-alarm flow

**What to build:** The human reported being raided with no option to defend, despite having alarms installed — but the mechanic already exists in code: with the `alarm` upgrade purchased (`Cultivating.add_alarm()`, `cultivating.gd:536`, `data/vein_alarm.json`), a successful raid attempt queues via `Raiding._queue_defend_raid()` (`systems/raiding.gd:430-436`) instead of resolving immediately, giving the player until the next tick to travel to the district and fight `Combat.start_defend_vein()` (`combat.gd:197`); if they don't show up in time, `_expire_pending_defend_raids()` (`raiding.gd:439-444`) resolves it exactly like the no-alarm path. This ticket is investigate-and-polish, not build-from-scratch: confirm the flow actually works end-to-end, and close the most likely real gap — the player isn't clearly told a defend-window has opened and is closing.

**Blocked by:** None — can start immediately.

**Status:** completed

- [x] Confirmed via existing test coverage: the pending-defend state is reachable, not a dead queue. `_queue_defend_raid()` pushes into `state.world.pendingDefendRaids`; `maybe_trigger_defend()` is wired into both `Travel.travel_to()` and `Sites.prospect()` (the two arrival chokepoints), so simply travelling to the district auto-starts the defend combat — `travel_to_triggers_the_defend_encounter_for_a_pending_alarmed_vein` and `prospect_on_the_days_last_block_still_triggers_the_defend_encounter...` (tests/test_raiding.gd) both cover this end-to-end. No screen change needed — arrival-triggered combat is the same UX shape every other district encounter already uses.
- [x] Already implemented pre-ticket: `_queue_defend_raid()` pushes "Alarm's gone off — %s are closing in on your vein in %s. Get there today to defend it." — distinct from the generic raid-loss line.
- [x] Was the actual gap. `resolve_raid_outcome()` now takes a `missed_defend` bool; `_expire_pending_defend_raids()` passes `true`, producing "Too late — %s took your vein in %s while the alarm was still ringing." instead of the plain "%s raided your vein in %s. It's theirs now." line every other loss path uses. Covered by `expired_defend_window_notification_reads_differently_from_a_plain_no_alarm_loss` (tests/test_raiding.gd).
- [ ] Manual check for the human: trigger a raid on an alarmed vein, confirm the "alarm's gone off" notification appears, travel to the district and confirm the defend combat starts automatically, and confirm both a win (vein untouched, no extra notification) and a loss (vein transfers, "raided... it's theirs now" notification) behave as expected. Also let one defend window expire untouched and confirm the new "Too late..." notification appears instead.
