# 08 — "Run" → "Leg it" rename

**What to build:** `scenes/screens/combat.gd`'s `_build_action_bar()` Run
button label changes to "Leg it".

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] Run button renders as "Leg it" in `combat.gd`'s `_build_action_bar()`
- [ ] Any other player-facing references to "Run" as this action's label (log lines, tooltips) reviewed for consistency
- [ ] `tests/test_combat_screen.gd` updated if it asserts on the old label text
- [ ] `PROSE-REVIEW`: new copy, tone bible per `docs/CONTENT-GUIDE.md`
